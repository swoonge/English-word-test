openai>=1.0.0
tkinterdnd2>=0.3.0
pyperclip>=1.8.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# 우분투/리눅스 사용자를 위한 추가 정보:
# tkinter 지원: sudo apt install python3-tk
# 클립보드 지원: sudo apt install xclip
# 드래그앤드롭은 선택적 기능입니다 (tkinterdnd2 설치 실패 시 자동 비활성화)
